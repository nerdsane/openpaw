# ARN-441 intent (temperpaw side)

Phase 4 of stage 3 (stack/docs/stage3-spec.md): make the SDLC a governed Temper
application. The execution unit stops living in PR comments, check runs, and bash
routing and becomes a queryable entity lifecycle - born at intent, carried to a
verified deploy, with orphan detection as a state-machine property.

This is the temperpaw (paw-patrol) slice. The canonical RFC and design chain are
in `arni-labs/stack` `docs/efforts/ARN-441/` (spec.md is the RFC). This folder is
the temperpaw PR's local chain for its planning/decision gates.

Source: Rita, 2026-08-29 ("isn't 3 the whole point of stage 3?"). Tracked as
Linear ARN-441 (team Arni-build, project TemperPaw).
