# Plan

1. Land each fix in the stack source first (single source of truth).
2. Re-vendor the five gate workflows here byte-identical.
3. Fix the fmt-red main in the same PR (it blocks the same checks job).
4. Panel-review the wave; merge once records validate and checks are green.
